apiVersion: apps/v1
kind: Deployment
metadata:
  name: cst-europartner-frontend
  namespace: cst-europartner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cst-europartner-frontend
  template:
    metadata:
      labels:
        app: cst-europartner-frontend
    spec:
      containers:
        - name: cst-europartner-frontend-container
          image: ghcr.io/oondemand/cst-europartner-frontend:${IMAGE_TAG}-prod
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.05"
            limits:
              memory: "128Mi"
              cpu: "0.1"
